#!/bin/sh
# wb-skill -- Skills mit Skripten je Agent, Welt und Bibliothek (docs/AGENTS-SKILLS.md).
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 "$SCRIPT_DIR/agents_skills.py" "$@"
