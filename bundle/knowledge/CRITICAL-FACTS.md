---
title: CRITICAL-FACTS
type: note
permalink: main/critical-facts
---

For future sessions: ALWAYS, every session, immediately — smallest mandatory context.

- User: {{USER_NAME}}, GitHub {{GITHUB_HANDLE}}, {{USER_EMAIL}}.
- Machine: {{MACHINE_DESCRIPTION}}, OS user `{{OS_USERNAME}}`. Projects in `{{PROJECTS_DIR}}`.
- Vault `~/Knowledge` = source of truth, git repo, private remote {{VAULT_REMOTE}}.
- Before tasks: search the vault (`rg` or Basic Memory MCP). After sessions: write a note.
- No emojis in apps/UIs. Commits only as {{GITHUB_HANDLE}}, no Claude co-author trailer.
- 90-secrets/: never read/reference/sync.
- Push/PR only the orchestrator, after confirmation.
- Details: [[INDEX]], [[HOT]], `10-global/` (user-profile, standing rules).
